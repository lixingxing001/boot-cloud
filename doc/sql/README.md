# SQL 模板说明

`doc/sql/` 目录用于放置脚手架的最小数据库基线。

当前提供：

- `boot-cloud-auth-mysql.sql`
  - OAuth2 client 管理表 `boot_cloud_oauth_client`

说明：

- 用户表、管理员表、租户域名表等通常需要与具体项目的用户中心、后台体系或租户体系对接，因此这里先不强行固化。
- 新项目如果采用脚手架默认的后台管理员认证与租户解析能力，需要再根据自身领域模型补充相应表结构。
